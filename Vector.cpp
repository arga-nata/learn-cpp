#include <iostream>
#include <vector>

using namespace std;

int main()
{
    vector<int> array;
    int nilai;

    array.push_back(nilai);

    // array.pop_back(nilai);

    array.size();

    array.empty();

    array.clear();

    array.at(1);

    array.operator[];

    return 0;
}