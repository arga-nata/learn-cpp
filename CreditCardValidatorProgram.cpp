#include <iostream>
using namespace std;
int getDigit(const int number);
int sumOddDigits(const string cardNumber);
int sumEvenDigits(const string cardNumber);

int main() {
  string cardNumber;
  int result = 0;

  cout << "Enter a credit card#: ";
  cin >> cardNumber;
  
  return 0;
}
int getDigit(const int number) { return 0; }
int sumOddDigits(const string cardNumber) { return 0; }
int sumEvenDigits(const string cardNumber) { return 0; }